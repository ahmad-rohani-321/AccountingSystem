# ViewModels Convention

- Place all request/response view models in this `ViewModels` folder.
- Use the namespace `AccountingSystem.ViewModels` for these classes.
- Keep domain/entity models in `Models/*`; only transport/view-model contracts belong here.
