@{
    ViewData["title"] = "اشخاص";
    ViewData["account-tab"] = "active";
    ViewData["accountindex"] = "active";
}
@section Styles {
    <link href="~/lib/tom-select/dist/css/tom-select.bootstrap5.css" rel="stylesheet" />
}
<div class="row row-deck row-cards">
    <div class="">
        <div class="card">
            <div class="card-header h2">
                د اشخاصو حسابونه
            </div>
            <div class="card-body">
            <div class="mb-3">
                <button class="btn btn-primary" id="createNewPersonAccountBtn">
                نوی شخص
                <i class="fas fa-plus ms-3"></i>
                </button>
            </div>
            <table class="table table-bordered table-hover table-responsive table-striped">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>نوم</th>
                        <th>کوډ</th>
                        <th>تذکره</th>
                        <th>لومړی شمېره</th>
                        <th>دوهم شمېره</th>
                        <th>ایمیل</th>
                        <th>ادرس</th>
                        <th>حساب ډول</th>
                        <th>فعالیت</th>
                        <th style="width: 100px;">کړني</th>
                    </tr>
                </thead>
                <tbody id="load-people-accounts-list">
                    
                </tbody>
            </table>
            </div>
        </div>
    </div>
</div>

@* start of new account form/modal *@
<div class="modal fade" id="createNewPersonAccount" tabindex="-1">
    <div class="modal-dialog modal-lg" role="document">
        <form id="newPersonAccountForm" class="modal-content overflow-auto">
            <div class="modal-header">
                <h5 class="modal-title">نوی شخص</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-header">
                <h3 class="h3">د شخص معلومات</h3>
            </div>
            <div class="modal-body">
                <div class="row mb-3">
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label" for="new-name">نوم</label>
                            <input type="text" class="form-control" id="new-name" placeholder="نوم" />
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label" for="new-code">کوډ</label>
                            <input type="text" class="form-control" id="new-code" placeholder="کوډ، مثلاً: ACC001" />
                        </div>
                    </div>
                </div>
                <div class="row mb-3">
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label" for="new-tazkira">تذکره</label>
                            <input type="text" class="form-control" id="new-tazkira" placeholder="تذکره" />
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label" for="new-first-phone">لومړی شمېره</label>
                            <input type="text" class="form-control" id="new-first-phone" placeholder="لومړی شمېره" />
                        </div>
                    </div>
                </div>
                <div class="row mb-3">
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label" for="new-second-phone">دوهم شمېره</label>
                            <input type="text" class="form-control" id="new-second-phone" placeholder="دوهم شمېره" />
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label" for="new-email">ایمېل</label>
                            <input type="text" class="form-control" id="new-email" placeholder="ایمېل" />
                        </div>
                    </div>
                </div>
                <div class="row mb-3">
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label" for="new-address">آدرس</label>
                            <input type="text" class="form-control" id="new-address" placeholder="آدرس" />
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label" for="new-person-type">حساب ډول</label>
                            <select id="new-person-type" class="form-control" placeholder="شخص ډول"></select>
                        </div>
                    </div>
                </div>
            </div>
            <div class="modal-header">
                <h3 class="h3">د شخص بلانسونه</h3>
            </div>
            <div class="modal-body">
                <table class="table table-bordered table-hover table-striped">
                    <thead>
                        <tr>
                            <th class="d-none">#</th>
                            <th>اسعارو نوم</th>
                            <th>مقدار</th>
                        </tr>
                    </thead>
                    <tbody id="listCurrencies">
                        <!-- Balance rows will be populated here -->
                    </tbody>
                </table>
            </div>
            <div class="modal-footer align-items-end">
                <button class="btn btn-primary" id="btnSaveNewPersonAccount" type="submit">
                    ثبت
                    <i class="fas fa-save ms-2"></i>
                </button>
                <button class="btn btn-danger" type="reset" data-bs-dismiss="modal">
                    لغوه
                    <i class="fas fa-times ms-2"></i>
                </button>
            </div>
        </form>
    </div>
</div>
@* end of new account form/modal *@

@* start of edit person account form/modal *@
<div class="modal fade" id="editPersonAccount" tabindex="-1">
    <div class="modal-dialog modal-lg" role="document">
        <form id="editPersonAccountForm" class="modal-content overflow-auto">
            <div class="modal-header">
                <h5 class="modal-title">نوی شخص</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-header">
                <h3 class="h3">د شخص معلومات</h3>
            </div>
            <div class="modal-body">
                <input type="hidden" id="edit-person-id" />
                <div class="row mb-3">
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label" for="edit-name">نوم</label>
                            <input type="text" class="form-control" id="edit-name" placeholder="نوم" />
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label" for="edit-code">کوډ</label>
                            <input type="text" class="form-control" id="edit-code" placeholder="کوډ، مثلاً: ACC001" />
                        </div>
                    </div>
                </div>
                <div class="row mb-3">
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label" for="edit-tazkira">تذکره</label>
                            <input type="text" class="form-control" id="edit-tazkira" placeholder="تذکره" />
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label" for="edit-first-phone">لومړی شمېره</label>
                            <input type="text" class="form-control" id="edit-first-phone" placeholder="لومړی شمېره" />
                        </div>
                    </div>
                </div>
                <div class="row mb-3">
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label" for="edit-second-phone">دوهم شمېره</label>
                            <input type="text" class="form-control" id="edit-second-phone" placeholder="دوهم شمېره" />
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label" for="edit-email">ایمېل</label>
                            <input type="text" class="form-control" id="edit-email" placeholder="ایمېل" />
                        </div>
                    </div>
                </div>
                <div class="row mb-3">
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label" for="edit-address">آدرس</label>
                            <input type="text" class="form-control" id="edit-address" placeholder="آدرس" />
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label" for="edit-person-type">حساب ډول</label>
                            <select id="edit-person-type" class="form-control" placeholder="شخص ډول"></select>
                        </div>
                    </div>
                </div>
            </div>
            <div class="modal-footer align-items-end">
                <button class="btn btn-primary" id="btnSaveEditedPersonAccount" type="submit">
                    ثبت
                    <i class="fas fa-save ms-2"></i>
                </button>
                <button class="btn btn-danger" type="reset" data-bs-dismiss="modal">
                    لغوه
                    <i class="fas fa-times ms-2"></i>
                </button>
            </div>
        </form>
    </div>
</div>
@* end of edit person account form/modal *@


@* start of account balance *@
<div class="modal fade" id="personBalancesModal" tabindex="-1">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content overflow-auto">
            <div class="modal-header">
                <h5 class="modal-title">نوی شخص</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-header">
                <h3 class="h3">د شخص معلومات <span Id="new-person-name" class="text-primary"></span></h3>
            </div>
            <div class="modal-body">
                <h3 class="h3">د شخص بلانسونه</h3>
                <table class="table table-bordered table-hover table-striped">
                    <thead>
                        <tr>
                            <th>اسعار</th>
                            <th>مقدار</th>
                        </tr>
                    </thead>
                    <tbody id="person-balances">
                        <!-- Balance rows will be populated here -->
                    </tbody>
                </table>
            </div>
            <div class="modal-footer align-items-end">
                <button class="btn btn-danger" type="reset" data-bs-dismiss="modal">
                    لغوه
                    <i class="fas fa-times ms-2"></i>
                </button>
            </div>
        </div>
    </div>
</div>
@* end of account balance *@

@* start of employee salery form/modal *@
<div class="modal fade" id="employeeSalery" tabindex="-1">
    <div class="modal-dialog modal-lg" role="document">
        <form id="employeeSaleryForm" class="modal-content overflow-auto">
            <div class="modal-header">
                <h5 class="modal-title">د میاشتې معاش ثبت</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-header">
                <h3 class="h3">کارمند: <span id="salery-employee-name" class="text-primary"></span></h3>
            </div>
            <div class="modal-body">
                <input type="hidden" id="salery-employee-id" name="id" />
                <div class="row mb-3">
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label" for="salery-month">میاشت</label>
                            <select id="salery-month" name="month" class="form-control">
                                <option value="">میاشت انتخاب کړئ</option>
                                <option value="1">وری</option>
                                <option value="2">غویی</option>
                                <option value="3">غبرګولی</option>
                                <option value="4">چنګاښ</option>
                                <option value="5">زمری</option>
                                <option value="6">وږی</option>
                                <option value="7">تله</option>
                                <option value="8">لړم</option>
                                <option value="9">لیندۍ</option>
                                <option value="10">مرغومی</option>
                                <option value="11">سلواغه</option>
                                <option value="12">کب</option>
                            </select>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label" for="salery-amount">د معاش مقدار</label>
                            <input type="number" id="salery-amount" name="amount" class="form-control" min="0" step="any" placeholder="مقدار" />
                            <div class="form-hint">معاش په اصلي اسعارو ثبت کیږي.</div>
                        </div>
                    </div>
                </div>
                <div class="mb-3">
                    <label class="form-label" for="salery-remarks">تفصیل</label>
                    <textarea id="salery-remarks" name="remarks" class="form-control" rows="3" placeholder="تفصیل"></textarea>
                </div>
            </div>
            <div class="modal-footer align-items-end">
                <button class="btn btn-primary" id="btnSaveEmployeeSalery" type="submit">
                    ثبت
                    <i class="fas fa-save ms-2"></i>
                </button>
                <button class="btn btn-danger" type="button" data-bs-dismiss="modal">
                    لغوه
                    <i class="fas fa-times ms-2"></i>
                </button>
            </div>
        </form>
    </div>
</div>
@* end of employee salery form/modal *@
@section Scripts {
<script src="~/lib/tom-select/dist/js/tom-select.complete.min.js"></script>
<script src="~/lib/just-validate/dist/just-validate.production.min.js"></script>
<script src="~/lib/moment-jalali/dist/jalali-moment.browser.js"></script>

<script>
$(document).ready(function() {
    // show new person account modal
    $("#createNewPersonAccountBtn").click(function(){
        $.ajax({
            url: "/api/Accounts/NextCode",
            type: "GET",
            success: function(data){
                $("#new-code").val(data);
            },
            error : function(response){
                showToast("warning", response.responseText);
            }
        });
        $("#createNewPersonAccount").modal('show');
    });


    var ts = new TomSelect("#new-person-type", {
        valueField: "id",
        labelField: "name",
        searchField: "name"
    });
    
    var edit_ts = new TomSelect("#edit-person-type", {
        valueField: "id",
        labelField: "name",
        searchField: "name"
    });
    // load account types 
    $.get("/api/Accounts/GetPeopleAccountTypes", function(data) {
        ts.addOptions(data);
        ts.refreshOptions(false);

        edit_ts.addOptions(data);
        edit_ts.refreshOptions(false);
    });
    // load currencies 
    $.ajax({
        url: "/api/Currency/Currencies/Simple",
        type: "GET",
        success: function(data) {
            var listCurrencies = $("#listCurrencies");
            data.forEach(function(currency) {
                var row = $("<tr>");
                row.append($("<td>").addClass("d-none").text(currency.id));
                row.append($("<td>").text(currency.name));
                row.append($("<td>").append($("<input>").attr("type", "number").attr("step", "any").attr("value", "0").addClass("form-control new-person-balance-validator").attr("placeholder", "مقدار")));
                listCurrencies.append(row);
            });
        },
    });
    // validate new person account form
    var newPersonAccountFormValidator = new JustValidate("#newPersonAccountForm");
    newPersonAccountFormValidator
    .addField("#new-name", [
        {
            rule: "required",
            errorMessage: "نوم اړین دی"
        }
    ])
    .addField("#new-code", [
        {
            rule: "required",
            errorMessage: "کوډ اړین دی"
        }
    ])
    .addField("#new-first-phone", [
        {
            rule: "required",
            errorMessage: "لومړی موبایل نمبر اړین دی"
        }
    ])
    .addField("#new-person-type", [
        {
            rule: "required",
            errorMessage: "حساب ډول اړین دی"
        }
    ]);
    // submit new person account form 
    newPersonAccountFormValidator.onSuccess(function(event) {
        event.preventDefault();
        var formData = {
            name: $("#new-name").val(),
            code: $("#new-code").val(),
            nic: $("#new-tazkira").val(),
            firstPhone: $("#new-first-phone").val(),
            secondPhone: $("#new-second-phone").val(),
            email: $("#new-email").val(),
            address: $("#new-address").val(),
            accountTypeId: ts.getValue(),
            balance: []
        };
        $("#listCurrencies tr").each(function() {
            var currencyId = $(this).find("td:first").text();
            var amount = $(this).find("input").val();
            if (amount) {
                formData.balance.push({ currencyID: currencyId, balance: parseFloat(amount) });
            }
        });
        $.ajax({
            url: "/api/Accounts/CreatePersonAccount",
            type: "POST",
            contentType: "application/json",
            data: JSON.stringify(formData),
            success: function(response) {
                showToast("success", "شخص حساب په بریالیتوب سره جوړ سو!");
                $("#newPersonAccountForm")[0].reset();
                $("#createNewPersonAccount").modal("hide");
                loadPeoplAccountList();
            },
            error: function(xhr, status, error) {
                showToast("warning", "د شخص حساب جوړولو کې ستونزه رامنځته شوه.");
            }
        });
    });

    // load people list 
    loadPeoplAccountList();
    function loadPeoplAccountList(){
        $.ajax({
            url : "/api/Accounts/PeopleAccount",
            type : "GET",
            success: function (data) {
                var dataTable = $("#load-people-accounts-list");
                dataTable.empty();

                data.forEach(function (rows) {
                    var row = $("<tr>");

                    $("<td>").text(rows.id).appendTo(row);
                    $("<td>").text(rows.name).appendTo(row);
                    $("<td>").text(rows.code).appendTo(row);
                    $("<td>").text(rows.nic).appendTo(row);
                    $("<td>").text(rows.firstPhone).appendTo(row);
                    $("<td>").text(rows.secondPhone).appendTo(row);
                    $("<td>").text(rows.email).appendTo(row);
                    $("<td>").text(rows.address).appendTo(row);
                    $("<td>").text(rows.accountTypeName).appendTo(row);
                    $("<td>").text(rows.isActive ? "فعال" : "غیر فعال").css("width", "100px").appendTo(row);

                    var actions = $("<td>");
                    if(rows.accountTypeId != 10){
                        var buttonGroup = $("<div>").addClass("btn-group-vertical");

                        $("<button>")
                            .text("تغیر")
                            .addClass("btn btn-sm btn-primary btn-edit-person-account")
                            .attr("data-row", JSON.stringify(rows))
                            .appendTo(buttonGroup);

                        $("<button>")
                            .text("بلانس")
                            .addClass("btn btn-sm btn-primary btn-view-person-account-balance")
                            .attr("data-info", JSON.stringify({ id: rows.id, name: rows.name }))
                            .appendTo(buttonGroup);
                        
                        if(rows.accountTypeId == 9){

                        $("<button>")
                            .text("معاش")
                            .addClass("btn btn-sm btn-primary btn-employee-salery")
                            .attr("data-info", JSON.stringify({ id: rows.id, name: rows.name }))
                            .appendTo(buttonGroup);

                        }

                        $("<button>")
                            .text(rows.isActive ? "غیر فعال" : "فعال")
                            .addClass(rows.isActive ? "btn btn-sm btn-danger btn-toggle-person-account-activation" : "btn btn-sm btn-success btn-toggle-person-account-activation")
                            .attr("data-id", rows.id)
                            .appendTo(buttonGroup);

                        actions.append(buttonGroup);
                    }
                        row.append(actions);
                        dataTable.append(row);
                    
                });
            },
            error : function(response){
                showToast("warning", response.responseText);
            }
        });
    }
    // validate edit person account form
    var editPersonAccountFormValidator = new JustValidate("#editPersonAccountForm");
    editPersonAccountFormValidator
    .addField("#edit-name", [
        {
            rule: "required",
            errorMessage: "نوم اړین دی"
        }
    ])
    .addField("#edit-code", [
        {
            rule: "required",
            errorMessage: "کوډ اړین دی"
        }
    ])
    .addField("#edit-first-phone", [
        {
            rule: "required",
            errorMessage: "لومړی موبایل نمبر اړین دی"
        }
    ])
    .addField("#edit-person-type", [
        {
            rule: "required",
            errorMessage: "حساب ډول اړین دی"
        }
    ]);
    // submit edit person account form
    editPersonAccountFormValidator.onSuccess(function(event) {
        event.preventDefault();
        var formData = {
            id: $("#edit-person-id").val(),
            name: $("#edit-name").val(),
            code: $("#edit-code").val(),
            nic: $("#edit-tazkira").val(),
            firstPhone: $("#edit-first-phone").val(),
            secondPhone: $("#edit-second-phone").val(),
            email: $("#edit-email").val(),
            address: $("#edit-address").val(),
            accountTypeId: edit_ts.getValue()
        };
        $.ajax({
            url: "/api/Accounts/UpdatePersonAccount",
            type: "PUT",
            contentType: "application/json",
            data: JSON.stringify(formData),
            success: function(response) {
                showToast("success", "شخص حساب په بریالیتوب سره تازه سو!");
                $("#editPersonAccountForm")[0].reset();
                $("#editPersonAccount").modal("hide");
                loadPeoplAccountList();
            },
            error: function(xhr, status, error) {
                showToast("warning", "د شخص حساب تازه کولو کې ستونزه رامنځته سوه.");
            }
        });
    });
    // edit person account information
    $(document).on("click", ".btn-edit-person-account", function(){
        var row = $(this).data("row");
        $("#edit-address").val(row.address);
        $("#edit-code").val(row.code);
        $("#edit-email").val(row.email);
        $("#edit-first-phone").val(row.firstPhone);
        $("#edit-second-phone").val(row.secondPhone);
        $("#edit-name").val(row.name);
        $("#edit-tazkira").val(row.nic);
        $("#edit-person-id").val(row.id);
        //$("#edit-person-type").val(row.accountTypeId).trigger("change");
        
        const tom = $("#edit-person-type")[0].tomselect;
        tom.setValue(row.accountTypeId);

        $("#editPersonAccount").modal("show");
    });
    // view person account balance
    $(document).on("click", ".btn-view-person-account-balance", function(){
        var info = $(this).data("info");
        $.ajax({
            url: "/api/Accounts/GetAccountBalance/" + info.id,
            type: "GET",
            success: function(response) {
                $("#new-person-name").text(info.name);
                var personBalances = $("#person-balances");
                personBalances.empty();
                response.forEach(function(balance) {
                    var row = $("<tr>");
                    row.append($("<td>").text(balance.currencyName));
                    row.append($("<td>").text(balance.balance));
                    personBalances.append(row);
                });
                $("#personBalancesModal").modal("show");
            },
            error: function(xhr, status, error) {
                showToast("warning", "د حساب بیلانس وچولو کې ستونزه رامنځته سوه.");
            }
        });
    });
    var salery_ts = new TomSelect("#salery-month", {
        create: false,
        allowEmptyOption: true
    });
    var employeeSaleryFormValidator = new JustValidate("#employeeSaleryForm");
    employeeSaleryFormValidator
    .addField("#salery-month", [
        {
            rule: "required",
            errorMessage: "میاشت انتخاب کړئ"
        },
        {
            validator: function(value){
                return Number.isInteger(Number(value)) && Number(value) >= 1 && Number(value) <= 12;
            },
            errorMessage: "صحیح میاشت انتخاب کړئ"
        }
    ])
    .addField("#salery-amount", [
        {
            rule: "required",
            errorMessage: "د معاش مقدار اړین دی"
        },
        {
            validator: function(value){
                return Number.isFinite(Number(value)) && Number(value) >= 0;
            },
            errorMessage: "مقدار باید منفي نه وي"
        }
    ]);
    employeeSaleryFormValidator.onSuccess(function(event) {
        event.preventDefault();
        var submitButton = $("#btnSaveEmployeeSalery");
        if (submitButton.prop("disabled")) return;
        var formData = {
            id: $("#salery-employee-id").val(),
            month: salery_ts.getValue(),
            amount: $("#salery-amount").val(),
            remarks: $("#salery-remarks").val()
        };
        if (Number(formData.id) <= 0 || !Number.isInteger(Number(formData.id))) {
            showToast("warning", "هیله ده د کارمند حساب انتخاب کړئ.");
            return;
        }
        submitButton.prop("disabled", true);
        $("#employeeSalery [data-bs-dismiss='modal']").prop("disabled", true);
        $.ajax({
            url: "/api/Journal/SaveEmployeeSalery?" + $.param(formData),
            type: "POST",
            success: function(response) {
                showToast("success", "د کارمند معاش په بریالیتوب سره ثبت سو!");
                $("#employeeSalery").modal("hide");
                loadPeoplAccountList();
            },
            error: function(response) {
                showToast("warning", response.responseText || "د معاش په ثبتولو کې ستونزه رامنځته سوه.");
            },
            complete: function() {
                submitButton.prop("disabled", false);
                $("#employeeSalery [data-bs-dismiss='modal']").prop("disabled", false);
            }
        });
    });
    $(document).on("click", ".btn-employee-salery", function(){
        if ($("#btnSaveEmployeeSalery").prop("disabled")) return;
        var info = $(this).data("info");
        $("#employeeSaleryForm")[0].reset();
        employeeSaleryFormValidator.refresh();
        $("#salery-employee-id").val(info.id);
        $("#salery-employee-name").text(info.name);
        salery_ts.setValue(String(moment().jMonth() + 1));
        $("#employeeSalery").modal("show");
    });
    // change account activation
    $(document).on("click", ".btn-toggle-person-account-activation", function(){
        var id = $(this).data("id");
        sweetAlert("تاسو اطمناني یاست؟", "تاسو اطمناني یاست، ترڅو انتخاب سوي حساب غیر فعال کړئ؟")
            .then((result) => {

                if (result.isConfirmed) {

                    $.ajax({
                        url: "/api/Accounts/UpdateAccountActivation/" + id,   // Your API endpoint
                        type: "PUT",

                        success: function (response) {

                            showToast("success", "د انتخاب سوي حساب فعالیت تغیر کړل سو.");

                            loadPeoplAccountList();
                        },

                        error: function (responsive) {
                            showToast("warning", responsive.responseText);
                        }
                    });

                }

            });
    });
});
</script>
}
